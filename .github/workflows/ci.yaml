name: CI Pipeline

on:
push:
branches:
-main:

jobs:
test:
runs on: ubuntu latest
steps:

-name: checkout code
uses: actions/checkout@v3

-name: setup Node.Js 
uses: actions/setup-node@v3

-name: Install Dependencies
run: npm install

-name: run test
run: npm test